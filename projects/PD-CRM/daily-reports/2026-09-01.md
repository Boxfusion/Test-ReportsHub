# QA Report — Monday 2026-09-01

**Module:** PD-CRM (Lesedi Service Management / CRM Admin Portal) — QA environment

**Headline:** PD-CRM stood up from scratch and 2 suites imported from Azure DevOps and executed the same day — **24 scripts, 24 executed** (suites 112731 Admin Portal Authentication, 112754 Case Creation) · **19 pass · 5 fail** · 6 defects logged, **4 of them in the ADO test cases rather than the application**.

| Cases driven | Passed | Failed | Defects logged | Suites executed |
|---|---|---|---|---|
| **24** | **19** | **5** | **6** | **2 of 7** |

> ⚠️ **Caveat.** Only the 8 authentication cases carry a persisted verdict on the dashboard. The 16 Case Creation results are known from console output but were lost before the report was written, and the suite needs a re-run to record them.

---

## 1. Module progress

| Module | New scripts | Re-run | Total run | Result | Overall progress |
|---|---|---|---|---|---|
| **PD-CRM** | **24** | 0 | 24 | 19 pass · 5 fail | **2 of 7 suites (29%)** |

*"New scripts" = test scenarios authored and driven for the first time. Both suites were imported from ADO Test Plan **112718** and executed the same day.*

**How overall progress is measured.** Progress is by suite, not by case, because the plan's total case count is not yet known — only 2 of its 7 leaf suites have been pulled from ADO. The 24 cases executed are the complete contents of those 2 suites. A per-case denominator will be available once the remaining 5 suites are imported.

---

## 2. What was covered

| # | Suite | ADO | Cases | Executed | Result |
|---|---|---|---|---|---|
| 1 | **Authentication — Admin Portal** | 112731 | 8 | 8 | 6 pass · 2 fail |
| 2 | **Case Management — Case Creation** | 112754 | 16 | 16 | 13 pass · 3 fail |

Reports: `projects/PD-CRM/test-reports/2026-09-01/` · defects: `test-reports/bugs/`.

**Environment:** the application was stable throughout. No outages or 500s across 24 runs.

---

## 3. Blocked — no test device

**Two of the seven suites cannot be started at all, because there is no test device available.**

| Suite | ADO | Cases | Status |
|---|---|---|---|
| **Authentication — CEP App** | 112732 | not pulled | 🔴 Blocked — requires a physical test device |
| **Authentication — SmartField App** | 112733 | not pulled | 🔴 Blocked — requires a physical test device |

Both are mobile application suites and cannot be exercised from the admin portal or a desktop browser. They were the reason today's testing moved to Case Management instead of finishing Authentication — the Admin Portal suite is the only one of the three Authentication suites that could be run.

**This is an impediment, not a scheduling choice.** Until a device is provisioned, Authentication cannot be reported as complete, and neither suite's case count can even be established. **A test device is needed to unblock this.**
